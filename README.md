libretro-prboom for Switch
===============

Port of prboom to libretro for Switch via libnx.

A custom retroarch.cfg provided in release prevents crashing with right analog stick use due to rewind/fast forward mapping.